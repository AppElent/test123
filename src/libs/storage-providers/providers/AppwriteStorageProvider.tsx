 
import BaseStorageProvider from './BaseStorageProvider';

export default class AppwriteStorage extends BaseStorageProvider {
  //private storage: Storage;
  //   constructor(options: any, providerOptions: any) {
  //     super(options, providerOptions);
  //     //   const client = new Client();
  //     //   client.setEndpoint('https://[APPWRITE_ENDPOINT]').setProject('[PROJECT_ID]');
  //     //   this.storage = new Storage(client);
  //   }
  //   async uploadFile(file: File, path: string): Promise<string> {
  //     //   const response = await this.storage.createFile('[BUCKET_ID]', file);
  //     //   return response.$id;
  //   }
  //   async getFile(fileId: string): Promise<File> {
  //     //   const response = await this.storage.getFileDownload('[BUCKET_ID]', fileId);
  //     //   const blob = await response.blob();
  //     //   return blob;
  //   }
  //   async deleteFile(fileId: string): Promise<void> {
  //     //   await this.storage.deleteFile('[BUCKET_ID]', fileId);
  //   }
}
